---
import GameLayout from "../../layouts/GameLayout.astro";
import { catalogGames, getRelatedGames } from "../../data/games";

export function getStaticPaths() {
  return catalogGames.map((game) => ({
    params: { slug: game.slug },
    props: {
      game,
      relatedGames: getRelatedGames(game.slug, 8)
    }
  }));
}

const { game, relatedGames } = Astro.props;
---
<GameLayout game={game} relatedGames={relatedGames} />

