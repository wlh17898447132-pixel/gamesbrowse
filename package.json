{
  "name": "gamesbrowse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.16.0"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "astro": "^5.18.1"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
